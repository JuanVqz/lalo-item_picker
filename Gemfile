source "https://rubygems.org"
git_source(:github) { |repo| "https://github.com/#{repo}.git" }

# Specify your gem's dependencies in lalo-item_picker.gemspec.
gemspec

gem "pg"

gem "sprockets-rails"

gem "debug", ">= 1.7.1", group: [:development, :test]
